# Data Schemas

The canonical Python schema definitions live in `orchestrator/schemas.py`.

Sprint 2 will expand this document with generation contracts, parquet columns, label taxonomy, and agent ownership notes.
