VITE_API_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000/ws/verdicts

# Set to "true" for a standalone build (e.g. the public Vercel demo) that runs entirely in the
# browser on simulated transactions, with no backend required. Leave unset/"false" for a real
# full-stack run against the orchestrator above.
VITE_DEMO_MODE=false
